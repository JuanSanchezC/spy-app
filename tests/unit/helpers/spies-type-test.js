import { spiesType } from 'spy-app/helpers/spies-type';
import { module, test } from 'qunit';

module('Unit | Helper | spies type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = spiesType([42]);
  assert.ok(result);
});
